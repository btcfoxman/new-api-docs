---
title: "AIによる呼び出しコードの生成"
description: "APIエンドポイント、リクエストボディ、レスポンス構造を手作業で解析する必要はありません。ドキュメントページで「ページをコピー」を使用し、ページ全体の内容をAIに送信するだけで完了します。"
---

## 推奨フロー

1. 対象の API ページを開き、ページ右上またはコンテキスト操作内の「ページをコピー」をクリックします。
2. コピーしたページ内容をそのまま GPT、Claude、またはその他のコード生成モデルに送信します。
3. ターゲット言語、ビジネスパラメータ、および必要なレスポンス処理方法を追記するだけで済みます。API パス、リクエストボディ、レスポンス構造を手動で整理する必要はありません。
4. 生成されたコードをローカルプロジェクトに配置した後、ドキュメントページに戻って Base URL、認証方式、および必須フィールドを再確認してください。

## プロンプトのヒント

```text
以下内容是我从 API 文档页直接复制的完整页面内容。
请基于这些内容生成 Python/Node.js/Go 调用代码。
要求：
1. 使用 Bearer Token 鉴权
2. Base URL 使用 https://api.aiid.edu.kg/v1
3. 保留完整请求体和错误处理
4. 用注释标明需要替换的 model、API Key 和业务参数
5. 若该接口是异步任务接口，补上查询任务结果的示例
```

## デプロイ前のチェック

- 正しい Base URL が入力されているか
- 正しい API パスが使用されているか
- `Authorization: Bearer YOUR_API_KEY` が含まれているか
- ドキュメントの要件に従って必須フィールドが渡されているか
