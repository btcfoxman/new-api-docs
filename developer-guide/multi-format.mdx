---
title: "多格式适配"
description: "OpenAI / Claude / Gemini 协议共存时的接入建议"
---

## 适配原则

1. 在业务层统一输入与输出结构。
2. 在网关层按协议差异做参数转换。
3. 保留上游原始错误码，便于排查。

## 实践建议

- 对话类优先抽象为 `messages` 结构。
- 图像和视频类统一为“提交任务 + 查询状态”。
- 为每类协议维护最小可运行示例，作为回归基线。
