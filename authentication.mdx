---
title: "鉴权说明"
description: "API Key 使用规范与安全建议"
---

## 鉴权方式

所有请求通过 `Authorization` 头传递 Bearer Token：

```http
Authorization: Bearer YOUR_API_KEY
```

## 安全建议

1. 不要将 API Key 写入前端代码或公开仓库。
2. 使用服务端密钥管理机制管理密钥。
3. 为不同环境使用不同 Key，并定期轮换。
4. 检测到泄露风险时，立即在控制台吊销并重建 Key。

