---
title: "Mã gọi API do AI tạo"
description: "Bạn không cần phải tự tách đường dẫn giao diện, cấu trúc body yêu cầu và phản hồi. Chỉ cần sử dụng tính năng \"Sao chép trang\" trực tiếp trên trang tài liệu và gửi toàn bộ nội dung cho AI."
---

## Quy trình đề xuất

1. Mở trang API mục tiêu, nhấp vào "Sao chép trang" ở góc trên bên phải hoặc trong menu ngữ cảnh.
2. Gửi trực tiếp toàn bộ nội dung trang đã sao chép cho GPT, Claude hoặc các mô hình tạo mã khác.
3. Chỉ cần bổ sung ngôn ngữ mục tiêu, tham số nghiệp vụ và cách xử lý phản hồi mong muốn; không cần tự tay sắp xếp lại đường dẫn API, body yêu cầu và cấu trúc phản hồi.
4. Sau khi đưa mã đã tạo vào dự án local, hãy quay lại trang tài liệu để đối chiếu Base URL, phương thức xác thực và các trường bắt buộc.

## Gợi ý Prompt

> Nội dung dưới đây là toàn bộ nội dung trang mà tôi đã sao chép trực tiếp từ trang tài liệu API.
> Vui lòng dựa trên các nội dung này để tạo mã gọi bằng Python/Node.js/Go.
> Yêu cầu:
> 1. Sử dụng Bearer Token để xác thực
> 2. Base URL sử dụng https://api.aiid.edu.kg/v1
> 3. Giữ nguyên đầy đủ phần thân yêu cầu và xử lý lỗi
> 4. Dùng chú thích để đánh dấu model, API Key và các tham số nghiệp vụ cần thay thế
> 5. Nếu giao diện này là API tác vụ bất đồng bộ, hãy bổ sung ví dụ truy vấn kết quả tác vụ

## Kiểm tra trước khi bàn giao

- Đã điền đúng Base URL chưa
- Đã sử dụng đúng đường dẫn API chưa
- Đã kèm theo `Authorization: Bearer YOUR_API_KEY` chưa
- Đã truyền các trường bắt buộc theo yêu cầu của tài liệu chưa
